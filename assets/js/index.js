const name = document.querySelector(".name");

// function testando(color) {
//     name.style.color = color;
// }

btn.addEventListener("click", testando);
function testando(color) {
    name.style.color = color
}

